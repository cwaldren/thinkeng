createGround(sim, opts)	Geometry creation, rotation math, static physics plane creation.
createBox(sim, opts)	Mesh matching + Cannon-es Box Extents sync.
createSphere(sim, opts)	Mesh matching + Cannon-es Radius sync.
createPlayer(sim, opts)	Keyboard WASD input listeners + velocity physics applying.
